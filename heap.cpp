// A heap is a data structure which allows efficient queries of min or max
// elements.  Without loss of generality, we will write a "max heap", which
// gives the greatest element in O(1) time and inserts new elements in O(log n)
// time.

// Is this really interesting from a template *function* perspective? This
// would be best implemented as a template *class*.

#include <iostream>
#include <vector>

using namespace std;

// Using templates.
template <typemname T>
vector<T> push_heap(vector<T> values) {
}

template<typename T>
vector<T> pop_heap(vector<T> values) {
}

int main() {
}
